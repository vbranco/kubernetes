apiVersion: v1
kind: Service
metadata:
  name: {{ include "playground_app.fullname" . }}
  labels:
    {{- include "playground_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "playground_app.selectorLabels" . | nindent 4 }}
