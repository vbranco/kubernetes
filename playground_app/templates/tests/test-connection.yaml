apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "playground_app.fullname" . }}-test-connection"
  labels:
    {{- include "playground_app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "playground_app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
